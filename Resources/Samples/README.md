# Samples layout

Per-voice folders under `Resources/Samples/<VoiceName>/`.

Naming: `<voice>_v1.wav` … `<voice>_v6.wav` (round-robin / random).
Optional snare pitch layers: `snare_p0_v1.wav` … (future).

Placeholder procedural WAVs ship for Linn voices so the engine loads.
Replace with real (licensed) multi-samples when ready — keep folder/name scheme.

CC0 / free packs: drop Kick + Snare first; engine picks up on next load.
